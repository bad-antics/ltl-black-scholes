// examples/demo.ltl — runnable demo for ltl_black_scholes

import std::io
use ltl_black_scholes as lib

fn main() {
    io::println("ltl_black_scholes demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}